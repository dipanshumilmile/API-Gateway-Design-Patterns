# API-Gateway-Design-Patterns

This project demonstrates API Gateway design patterns in Java, now with a desktop UI.

## Run

Compile:

```bash
javac Main.java
```

Launch desktop UI (default mode):

```bash
java Main
```

Launch console mode:

```bash
java Main --cli
```